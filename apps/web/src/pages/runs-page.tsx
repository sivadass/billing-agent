import { PageHeader, Typography } from 'cleanplate';

export function RunsPage() {
  return (
    <>
      <PageHeader
        title="Runs"
        subtitle="Track job executions and drill into run details."
      />
      <Typography variant="p" margin="t-3">
        Runs hub coming in the next task.
      </Typography>
    </>
  );
}
